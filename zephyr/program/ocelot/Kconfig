# Copyright 2025 The ChromiumOS Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

config BOARD_OCELOTRVP_ITE
	bool "Google Ocelot ITE (it82002) add-in card for the RVP"
	help
	  Build Google ITE it82002 based Ocelot add-in card.
	  This board is for use with the Intel RVP using the
	  MECC 2.1 connector.

config BOARD_OCELOTRVP_NPCX
	bool "Google Ocelot Nuvoton (npcx9m7f) add-in card for the RVP"
	help
	  Build Google Nuvoton npcx9m7f based Ocelot add-in card.
	  This board is for use with the Intel RVP using the
	  MECC 2.1 connector.

config BOARD_OCELOTRVP_MCHP
	bool "Google Ocelot with microchip (mec1727) EC for the RVP"
	help
	  Build Microchip EC based Ocelot RVP.
	  This board is for use with the Intel RVP using the
	  on board EC.

config BOARD_OCELOTRVP_RTK
	bool "Google Ocelot Realtek (rtk5915) add-in card for the RVP"
	help
	  Build Realtek rtk5915 based Ocelot add-in card.
	  This board is for use with the Intel RVP using the
	  MECC 2.1 connector.

config BOARD_OJAL
	bool "Google Ocelot with Realtek (rtk5915) EC"
	help
	  Build Google Ojal reference board.
	  This board uses an Intel WCL SoC and RTK EC.

config BOARD_MATSU
	bool "Google Ocelot with ITE (it82002) EC"
	help
	  Build Google Matsu reference board.
	  This board uses an Intel WCL SoC and ITE EC.

config BOARD_KODKOD
	bool "Google Ocelot with Realtek (rtk5915) EC"
	help
	  Build Google Kodkod reference board.
	  This board uses an Intel WCL SoC and RTK EC.

config BOARD_OCICAT
	bool "Google Ocelot with ITE (it82002) EC"
	help
	  Build Google Matsu reference board.
	  This board uses an Intel WCL SoC and ITE EC.

module = Oceolot
module-str = Ocelot board-specific code
source "subsys/logging/Kconfig.template.log_config"

source "Kconfig.zephyr"
